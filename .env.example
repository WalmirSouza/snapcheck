# Connection string padrão para desenvolvimento local com docker-compose
ConnectionStrings__PostgreSQL=Host=localhost;Port=5432;Database=snapcheck;Username=snapcheck;Password=snapcheck

# Token do bot (opcional — também pode ser configurado pela interface web)
# Telegram__Token=
