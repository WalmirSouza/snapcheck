# Modelo — NÃO commitar com valores reais. Copie para secret.yaml (já
# ignorado pelo .gitignore) e preencha antes de aplicar num cluster real.
apiVersion: v1
kind: Secret
metadata:
  name: snapcheck-secrets
type: Opaque
stringData:
  ConnectionStrings__PostgreSQL: "Host=SUBSTITUIR;Port=5432;Database=snapcheck;Username=SUBSTITUIR;Password=SUBSTITUIR"
  Admin__ApiKey: "SUBSTITUIR-por-uma-chave-forte-gerada-para-producao"
